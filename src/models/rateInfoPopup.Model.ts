declare module rateInfoPopup.Models {
    
        /**
         * Modelo para las direcciones de cliente
         */
        export interface IRateInfoPopupVarsModel {
            size: string;
            visible: boolean;
            closeEnabled: boolean;
            tarifa: any;
        }
    }