declare module rateDeviceSelectionPopup.Models {

    /**
     * Modelo para las direcciones de cliente
     */
    export interface IRateDeviceSelectionPopupVarsModel {
        size: string;
        visible: boolean;
        closeEnabled: boolean;
        tarifas: any;
    }
}